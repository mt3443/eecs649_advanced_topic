RNNoise is a noise suppression library based on a recurrent neural network

To compile, just type:
% ./autogen.sh
% ./configure
% make

Once compiled, you can remove noise from a .wav file using the following command:

% ./remove_noise.sh <input_file> <output_file>

For example:

% ./remove_noise.sh ../examples/before.wav ../examples/after_rnnoise.wav
